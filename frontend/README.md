# SpaceHub frontend

React + Vite client for the room booking API.

See the [root README](../README.md) for full setup, env vars, and features.

```bash
npm install
npm run dev
```

Requires `VITE_API_URL` (e.g. `http://localhost:3000/`) and the backend running on port 3000.
